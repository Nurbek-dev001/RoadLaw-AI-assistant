# 🚗 ПДД AI Казахстан - ЗАПУЩЕНО ✅

## ✨ Статус

**✅ Backend (FastAPI):** Запущен на http://localhost:8000  
**✅ Frontend (Streamlit):** Запущен на http://localhost:8501

Оба приложения работают и готовы к использованию!

---

## 🔧 Решение проблемы

### Проблема
Команда `python main.py` не работала и выводила только "Python" с exit code 1.

### Причина
На Windows PowerShell команда `python` не была правильно настроена, хотя Python был установлен. Баш-команда `python` работает не везде, особенно в PowerShell.

### Решение
Использовать `py -3.12` - официальный Windows Python launcher вместо `python`.

```bash
# ❌ Не работает
python main.py

# ✅ Работает
py -3.12 main.py
streamlit run frontend/app.py
```

---

## 🚀 Как запустить

### Вариант 1: Автоматически (Windows)

```bash
start_windows.bat
```

Откроет 2 окна и запустит оба приложения автоматически.

### Вариант 2: Вручную (2 терминала)

**Терминал 1 - Backend:**
```bash
cd backend
py -3.12 main.py
```

**Терминал 2 - Frontend:**
```bash
py -3.12 -m streamlit run frontend/app.py
```

Затем откройте: **http://localhost:8501**

---

## 🌐 Адреса

| Компонент | Адрес | Описание |
|-----------|-------|---------|
| **Streamlit UI** | http://localhost:8501 | Интерфейс для вопросов |
| **FastAPI Backend** | http://localhost:8000 | API сервер |
| **API Документация** | http://localhost:8000/docs | Swagger UI |
| **Health Check** | http://localhost:8000/health | Статус сервера |

---

## 📝 Примеры использования

Отправьте вопрос в интерфейсе: 

```
🔍 Спросить:
"Кто уступает на круге?"

💬 Ответ:
Согласно ПДД РК, пункт 13.7: водитель, который движется по 
кольцевой дороге, имеет приоритет перед автомобилями, входящими 
на кольцо...

📋 Источник: 
Раздел 13, Пункт 7
```

### Другие примеры вопросов:

- "Какой штраф за проезд на красный свет?"
- "Можно ли обгонять на пешеходном переходе?"
- "Какая максимальная скорость в городе?"
- "Правила перестроения на перекрестке"
- "Знаки дорожного движения"

---

## 🔌 API Примеры

### Спросить вопрос

```bash
curl -X POST http://localhost:8000/ask \
  -H "Content-Type: application/json" \
  -d '{"question": "Кто уступает на круге?"}'
```

**Ответ:**
```json
{
  "answer": "Согласно ПДД РК, пункт 13.7...",
  "sources": [
    {
      "section": "13. Проезд перекрестков",
      "id": "13.7",
      "title": "Приоритет на круговом движении",
      "relevance": 0.95
    }
  ],
  "confidence": 0.92
}
```

### Проверить статус

```bash
curl http://localhost:8000/health
```

**Ответ:**
```json
{
  "status": "healthy"
}
```

---

## 📊 Архитектура

```
┌─────────────────────────────────────┐
│  Браузер (http://localhost:8501)   │
│  Streamlit Frontend                │
└──────────────┬──────────────────────┘
               │ HTTP POST /ask
               ↓
┌─────────────────────────────────────┐
│  FastAPI Backend (port 8000)       │
│  ├─ RAG поиск в документах          │
│  ├─ чтобы не галлюцинировать        │
│  └─ возвращает ответ с источником   │
└─────────────────────────────────────┘
```

---

## 🎯 Следующие шаги

### Фаза 2 - Улучшения

- [ ] Добавить поддержку Казахского языка (KZ)
- [ ] Режим "Экзамен" с тестами
- [ ] Картинки перекрестков
- [ ] Голосовой помощник
- [ ] История вопросов

### Фаза 3 - Интегрирация

- [ ] Telegram бот
- [ ] Мобильное приложение
- [ ] На домене: https://pdd-ai.kz

### Production Deploy

- [ ] Backend: Render.com
- [ ] Frontend: Streamlit Cloud
- [ ] Custom домен

---

## 📁 Файлы проекта

```
RoadLaw AI/
├── backend/
│   ├── main.py           ← FastAPI сервер ✅
│   └── prepare_data.py   ← Подготовка данных
├── frontend/
│   └── app.py            ← Streamlit UI ✅
├── data/
│   └── pdd_sample.json   ← Примеры ПДД
├── .env                  ← Конфиг
├── .streamlit/
│   └── secrets.toml      ← Streamlit секреты
├── start_windows.bat     ← Автозапуск (Windows)
├── start_unix.sh         ← Автозапуск (Unix)
└── requirements.txt      ← Python зависимости
```

---

## 🛠️ Troubleshooting

### Ошибка: "Не могу подключиться к серверу"

1. Убедитесь Backend запущен: `http://localhost:8000/health`
2. Переезапустите: Ctrl+C и заново `py -3.12 main.py`

### Ошибка: "Port 8000/8501 already in use"

```bash
# Найти процесс
netstat -ano | findstr :8000

# Убить процесс (замените PID)
taskkill /PID <PID> /F
```

### Frontend не может подключиться к Backend

Проверьте в Streamlit консоли если есть CORS ошибка. Backend должен иметь:
```python
CORSMiddleware(allow_origins=["*"])
```

Это уже есть в main.py, так что должно работать.

---

## ⚠️ Важно

- **Это справочный сервис**, не юридическая консультация
- **Без ИИН и персональных данных**
- Проверяйте информацию по официальным источникам
- При ДТП - обратитесь в МВД

---

## 📞 Поддержка

**Если что-то не работает:**

1. Проверьте что оба процесса запущены (посмотрите терминалы)
2. Обновите статус в 30 секунд (кэширование)
3. Перезапустите оба приложения
4. Проверьте логи

---

**🎉 MVP ПДД AI Казахстан готов к использованию!**

Откройте http://localhost:8501 в браузере и начните задавать вопросы.
